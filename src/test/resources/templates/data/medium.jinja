{% import 'helper.j2' as macro %}

# Data Narrative

## Data Query

**Data Catalog**: {{Catalog.label}} {{Catalog.comment}}
**Data query explanation**: {{Query.comment}}


{{Catalog.query.value}}


## Data retrieved

The query returns information for the {{VariableBinding|length}} variables. The variables are:

{{helper.create_table_binding(VariableBinding)}}

### Input files

The query returns {{Resource|length}} resources. The resources are: {{helper.create_table_from_list(Resource)}}


